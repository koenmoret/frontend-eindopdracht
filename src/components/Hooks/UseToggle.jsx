import React from 'react';

const UseToggle = () => {
    const setRegister = isRegister);
    const setLogin = isLogin);
};

export default UseToggle;
// function toggleForm({isRegister,setRegister,isLogin,setLogin}){
//     setRegister(!isRegister);
//     setLogin(!isLogin);
// };
//
// export default toggleForm()